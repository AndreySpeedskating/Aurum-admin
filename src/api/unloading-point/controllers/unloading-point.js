'use strict';

/**
 * unloading-point controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::unloading-point.unloading-point');
